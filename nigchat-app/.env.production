# Production build target.
#
# wss:// not ws:// — the app talks to an HTTPS origin, and an insecure socket
# to a secure host fails silently.
EXPO_PUBLIC_API_URL=https://api.nigchat.com
EXPO_PUBLIC_WS_URL=wss://api.nigchat.com
